server {
    listen 80;           # слушает все интерфейсы
    server_name ailib.tau-edu.kz;       # любой hostname

    root /usr/share/nginx/html;
    index index.html;

    # фронт
    location / {
        try_files $uri /index.html;
    }

    # API
    location /api/ {
        proxy_pass http://web:8000;   # web — имя контейнера бэка
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # FastAPI docs
    location /docs/ {
        proxy_pass http://web:8000/docs/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

server {
    listen 80;           # слушает все интерфейсы
    server_name ailibrary.tau-edu.kz;       # любой hostname

    root /usr/share/nginx/html;
    index index.html;

    # фронт
    location / {
        try_files $uri /index.html;
    }

    # API
    location /api/ {
        proxy_pass http://web:8000;   # web — имя контейнера бэка
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # FastAPI docs
    location /docs/ {
        proxy_pass http://web:8000/docs/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
